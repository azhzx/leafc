#lang("builtin_i32_type")
type Int32;

#lang("builtin_i64_type")
type Int64;

#lang("builtin_never_type")
type Never;

type Maybe
    | Just of Int32
    | Nothing


fun main()
    let a = Maybe.Just(100)
    if a is Maybe.Just(binding _a)
        let n = _a