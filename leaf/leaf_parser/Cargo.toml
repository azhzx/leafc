[package]
name = "leaf_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
leaf_lexer = { path = "../leaf_lexer" }
leaf_preprocessor = { path = "../leaf_preprocessor" }
leaf_coreapi = { path = "../leaf_coreapi" }
intervaltree = "0.2.7"